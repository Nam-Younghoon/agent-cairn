# ARCHITECTURE — <기능 또는 버그 제목>

<!--
작성 규칙:
  - 구조 변경이 있거나, 작업을 이해하려면 전체 구조 파악이 필요한 경우에만 작성.
  - 각 레이어의 **의존성 방향**을 명시: 안쪽(domain) ← 바깥쪽(infrastructure).
  - 기존 구조를 모르면 parallel-explorer 로 먼저 조사 후 작성.
-->

## 현재 구조 요약
<!-- 관련 모듈·레이어를 2~3문단 또는 트리/시퀀스 스케치로 요약. -->

```
<간단한 트리 또는 시퀀스>
```

## 의존성 규칙
<!--
프로젝트의 의존성 방향을 적어둔다. 이번 변경이 이 규칙을 어기지 않도록 유의.
예: routes → controllers → services → domain
    infrastructure/repositories → domain/repositories (인터페이스)
    domain 은 어떤 바깥 레이어도 참조하지 않는다.
-->

## 이번 변경이 속하는 위치
- 어느 계층/모듈에 들어가는가:
- 왜 그 위치가 맞는가:
- 다른 위치 대안은 왜 기각했는가 (있으면):

## 영향받는 모듈
<!-- path 를 명시하고 변경 내용·이유를 1줄씩. -->
- `<path>`: 변경 내용 / 이유
- `<path>`: ...

## 데이터 흐름
<!-- 입력 → 처리 → 저장/응답 흐름을 단계별로. 필요 시 시퀀스 다이어그램 스케치. -->
1. ...
2. ...
3. ...

## 인터페이스 / 계약 변경
<!--
아래 중 하나라도 변경되면 명시:
  - HTTP API 엔드포인트/응답 스키마
  - 이벤트 페이로드
  - DB 테이블/컬럼
  - 공용 타입/DTO
  - 외부 SDK 노출 인터페이스
호환성 (breaking / backward compatible) 도 표기.
-->

## 비기능 영향
<!-- 성능, 지연, 리소스 사용, 장애 전파 경로, 관측성. -->

## 대안과 기각 이유
<!-- 여러 구조를 검토했다면 요약. 상세는 ADR.md 로 분리. -->
